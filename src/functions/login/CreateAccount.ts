// Function that takes the entered username and password and handles the login process and validation
export function CreateAccount(username: string, password: string) {
  console.log("CreateAccount Function Activated")
  return;
}