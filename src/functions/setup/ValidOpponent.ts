async function checkUsername(username: string) {
  return;
}

export function ValidOpponent(username: string): boolean {
  //TODO Write this function to check if the entered username matches any account usernames in the database
  return false;
}